# Airline-Demand-Forecasting
Weekday-sensitive forecasting model that predicts final airline booking demand from partial data - 24% lower error than naive baseline (MASE 0.64 vs 0.84). Python/pandas.
